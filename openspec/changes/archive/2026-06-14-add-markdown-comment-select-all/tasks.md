## 1. 评论选择状态

- [x] 1.1 在 `FilePreviewPanel` 中基于当前 `markdownComments` 和 `selectedCommentIds` 派生全部评论 ID、已全选状态和部分选中状态。
- [x] 1.2 新增评论列表全选切换处理：已全选时清空选择，未全选或部分选中时选中当前文件全部评论。
- [x] 1.3 为工具栏全选 checkbox 同步 `indeterminate` DOM 状态，并设置清晰的 `aria-label` / `aria-checked` 语义。

## 2. 评论列表工具栏 UI

- [x] 2.1 在 Markdown 评论列表工具栏左侧渲染全选 checkbox 和当前选择状态文案。
- [x] 2.2 保持现有“发送选中”和“删除”按钮行为不变，并确保未选中任何评论时继续禁用。
- [x] 2.3 调整评论列表工具栏样式，使全选控件、状态文案和批量操作按钮在窄面板内不溢出。

## 3. 行为与回归验证

- [x] 3.1 验证 0 条评论、1 条评论、多条评论时，全选控件显示和切换行为符合规格。
- [x] 3.2 验证部分选中状态下全选控件呈现 mixed 状态，点击后选中当前文件全部评论。
- [x] 3.3 验证全选后发送选中、删除选中仍只作用于当前文件评论，且不改变 Markdown 源文件或评论 payload 格式。
- [x] 3.4 运行类型检查，确认 `FilePreviewPanel` 和相关样式改动无 TypeScript 错误。
