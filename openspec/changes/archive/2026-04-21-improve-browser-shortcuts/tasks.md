## 1. 快捷键分发基础设施

- [x] 1.1 调整应用级快捷键分发逻辑，为 `Cmd+L` 提供不受地址栏等可编辑元素屏蔽的保留开关键语义
- [x] 1.2 为浏览器面板建立固定的上下文快捷键分发表，在浏览器打开时拦截 `Cmd+T`、`Cmd+W`、`Cmd+Shift+[`、`Cmd+Shift+]`、`Cmd+1..9`、`Cmd+R`、`Cmd+[`、`Cmd+]`
- [x] 1.3 在主进程为浏览器 `webview` 增加键盘事件桥接，将网页内容获焦时的浏览器快捷键通过 IPC 转发回渲染进程

## 2. 浏览器面板动作收口

- [x] 2.1 在 `BrowserPanel` 中补齐基于快捷键的新建/关闭/切换/编号跳转标签页能力，并统一复用现有 tab 状态管理
- [x] 2.2 在 `BrowserPanel` 中补齐基于快捷键的刷新、后退、前进行为，并确保地址栏、空状态页和网页内容下行为一致
- [x] 2.3 修正 `browser:open-url` 处理链路，使网页请求新窗口时自动打开浏览器面板并激活新建标签页

## 3. 冲突行为与交互验证

- [x] 3.1 验证浏览器打开时冲突快捷键优先作用于浏览器，浏览器关闭时恢复现有 terminal / workspace 行为
- [x] 3.2 验证 `Cmd+L` 在浏览器关闭、地址栏获焦、网页内容获焦三种状态下都能切换浏览器面板
- [x] 3.3 验证最后一个浏览器标签页关闭后的空状态、编号跳转越界静默处理以及前进/后退历史边界行为
