## 1. 快捷键基础设施

- [x] 1.1 新增应用级快捷键配置模型，定义 3 个动作的固定 action id 与默认绑定值
- [x] 1.2 在应用根部实现快捷键注册中心，统一处理键盘监听、动作注册、命中分发与注销
- [x] 1.3 接入快捷键配置持久化读取与保存逻辑，保证首次启动使用默认值、后续启动恢复用户配置

## 2. 设置面板

- [x] 2.1 在 `App` 根部实现应用内设置面板容器，并支持通过 `Command + ,` 打开/关闭
- [x] 2.2 在设置面板中实现快捷键配置区，展示文件树显隐、terminal 侧边栏显隐、新增 workspace 这 3 个配置项
- [x] 2.3 实现快捷键录入草稿、重复绑定校验、空绑定校验以及保存/关闭交互

## 3. 动作接入

- [x] 3.1 将文件树模块展示/收起接入快捷键中心，并保持现有分栏收起/展开行为一致
- [x] 3.2 将 `TerminalPanel` 的侧边栏展示/收起动作接入快捷键中心
- [x] 3.3 将 `TerminalPanel` 的新增 workspace 动作接入快捷键中心，并保持与现有 `+` 按钮一致的创建结果

## 4. 收尾验证

- [x] 4.1 确认设置保存后新的快捷键立即生效，且重新启动应用后仍能恢复
- [x] 4.2 确认在设置面板录入态或普通文本输入焦点下不会误触发全局快捷键动作
