## 1. 数据模型与持久化

- [x] 1.1 定义二维码工具记录类型、存储版本和 `localStorage` key。
- [x] 1.2 实现加载存储数据的校验与容错，损坏数据回退为空记录。
- [x] 1.3 实现保存最近记录和固定项的通用写入逻辑，保持 renderer 状态与 `localStorage` 同步。

## 2. 最近生成记录

- [x] 2.1 在二维码成功生成后添加稳定延迟保存，输入继续变化时取消旧保存。
- [x] 2.2 实现最近记录按文本去重、更新时间、移动到顶部和最多保留 10 条。
- [x] 2.3 实现点击最近记录回填二维码输入并复用现有生成流程。
- [x] 2.4 实现删除单条最近记录和清空最近记录，且不影响当前输入与固定项。

## 3. 固定常驻链接

- [x] 3.1 在当前二维码输入非空且生成成功时提供固定入口。
- [x] 3.2 实现固定项跨会话保存、按文本去重和更新时间。
- [x] 3.3 实现点击固定项回填输入并重新生成二维码预览。
- [x] 3.4 实现取消固定，且不清空当前输入、不删除同文本最近记录。

## 4. 二维码工具界面

- [x] 4.1 在二维码工具输入侧增加固定常驻链接和最近生成记录区域。
- [x] 4.2 为记录项提供紧凑展示、长文本截断、完整内容 title 和删除 / 取消固定操作。
- [x] 4.3 调整布局滚动约束，确保记录较多时不挤压输入区和二维码预览到不可用。
- [x] 4.4 保持现有二维码生成、空状态、错误状态和复制 SVG 行为不回归。

## 5. 验证

- [x] 5.1 验证最近记录保存、最多 10 条、重复生成去重和跨会话恢复。
- [x] 5.2 验证固定项保存、重复固定去重、取消固定和不计入最近记录上限。
- [x] 5.3 验证删除 / 清空记录不清空当前二维码输入。
- [x] 5.4 运行 `npm run lint`。
