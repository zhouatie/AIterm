## 1. 修复笔记编辑器任务列表渲染

- [x] 1.1 检查 `NoteEditor` 的 Milkdown preset / plugin 组合，补齐使标准 GFM 任务列表在编辑态输出 checkbox 结构所需的配置
- [x] 1.2 调整笔记编辑器相关样式或节点渲染，确保 `- [ ]`、`- [x]`、`- [X]` 在加载已有内容和持续输入时都显示为可见 checkbox 任务项
- [x] 1.3 验证笔记编辑器内容回调仍输出标准 Markdown 字符串，且任务列表渲染修复不会破坏撤销重做和普通列表编辑

## 2. 修复笔记面板保留快捷键分发

- [x] 2.1 调整笔记面板展示/收起快捷键的分发逻辑，使其在笔记编辑器和笔记搜索输入框获焦时仍能触发面板显隐
- [x] 2.2 让输入态下的保留开关键跟随 `toggle-notes` 的当前绑定值，而不是只识别默认 `Command + O`
- [x] 2.3 确保命中保留开关键后阻止事件继续交给编辑器或搜索输入框处理，并保持笔记面板与其他 overlay 的互斥行为不回归

## 3. 回归验证

- [x] 3.1 手动验证打开已有含任务列表的笔记、输入新的任务列表标记、切换已完成/未完成任务项的可见渲染表现
- [x] 3.2 手动验证笔记面板在关闭状态、编辑器获焦状态、搜索框获焦状态下都可通过当前快捷键绑定切换显隐
- [x] 3.3 若项目已有相关测试基础设施，补充或更新覆盖任务列表渲染与笔记面板保留快捷键行为的测试
