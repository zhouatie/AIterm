## 1. 二维码固定操作

- [x] 1.1 增加按当前二维码文本取消固定的状态更新逻辑，保持固定列表和 `localStorage` 同步。
- [x] 1.2 将二维码工具主按钮改为未固定时执行固定、已固定时执行取消固定。
- [x] 1.3 调整主按钮文案、图标和 tooltip，使已固定状态明确呈现“取消固定”。
- [x] 1.4 保持固定列表内取消固定入口、最近记录和复制 SVG 行为不回归。

## 2. 验证

- [x] 2.1 验证当前文本固定后可通过主按钮取消固定，且不清空输入。
- [x] 2.2 验证取消固定不删除同文本最近生成记录。
- [x] 2.3 运行 TypeScript 检查或等价 focused 检查，确认修改无类型错误。
