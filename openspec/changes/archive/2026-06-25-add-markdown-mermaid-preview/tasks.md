## 1. Mermaid 代码块渲染

- [x] 1.1 在 `MarkdownPreview` 中识别 `language-mermaid` fenced code block，并将其分流到 Mermaid 图表组件。
- [x] 1.2 使用现有 `mermaid` 依赖实现图表异步渲染，支持多个图表块独立渲染，并避免过期渲染结果覆盖最新内容。
- [x] 1.3 接入 `ThemeContext`，让 Mermaid 图表随当前 light/dark 主题选择对应渲染主题。
- [x] 1.4 为 Mermaid 渲染失败状态展示错误反馈，并保留源码可见。

## 2. 预览布局与既有行为

- [x] 2.1 为 Mermaid 图表补充预览样式，保证 SVG 在 Markdown 预览区内可滚动、可读且不撑破布局。
- [x] 2.2 确认普通代码块仍保留语法高亮和复制控件，任务 checkbox、评论、查找和标题导航行为不被 Mermaid 分流影响。

## 3. 验证

- [x] 3.1 运行可用的类型检查或构建验证，确认新增代码通过项目现有检查。
- [x] 3.2 使用包含有效 Mermaid、无效 Mermaid 和普通代码块的 Markdown 内容验证预览渲染路径。

## 4. 滚动渲染回归修复

- [x] 4.1 隔离 Markdown 正文渲染与滚动驱动的评论/选择覆盖层状态，避免滚动触发 `ReactMarkdown` 和 Mermaid 图表重渲染。
- [x] 4.2 在无评论标记需要重新定位时，滚动预览不更新评论布局状态。
- [x] 4.3 验证滚动场景下 Mermaid 图表不会重新进入 loading 或重复调用渲染，同时保留普通代码块、任务 checkbox、评论和标题导航行为。

## 5. Mermaid 全屏查看与缩放

- [x] 5.1 新增共享 Mermaid 图表查看组件，支持内嵌展示、点击打开全屏查看层、关闭查看层和滚轮缩放。
- [x] 5.2 将 Markdown 预览中的 Mermaid 图表接入共享查看组件，保留渲染失败、滚动稳定性和普通代码块行为。
- [x] 5.3 将工具面板 Mermaid 预览接入共享查看组件，保留输入渲染、错误展示和主题跟随行为。
- [x] 5.4 验证 Markdown 预览和 Mermaid 工具均可点击全屏查看，且全屏查看中滚轮可放大缩小并受限于合理范围。

## 6. 全屏源码模式与默认适配

- [x] 6.1 扩展共享 Mermaid 图表查看组件，支持传入 Mermaid 源码并在全屏查看层切换图表模式 / 源码模式。
- [x] 6.2 在全屏图表模式打开时根据 SVG 尺寸和画布尺寸计算初始等比缩放，使图表默认撑满可用宽高且不变形。
- [x] 6.3 将 Markdown 预览 Mermaid 图表和 Mermaid 工具预览都传入源码，确保两处全屏查看均可切换源码与图表。
- [x] 6.4 验证源码 / 图表切换、默认等比适配、滚轮缩放和现有全屏关闭行为。

## 7. 全屏交互修正与复制

- [x] 7.1 修正全屏图表 / 源码切换控件的视觉边界与点击命中区域对齐问题。
- [x] 7.2 改进全屏图表默认适配逻辑，使图表打开后在保持宽高比的前提下尽量占满容器。
- [x] 7.3 增加全屏一键复制能力，图表模式复制 SVG，源码模式复制 Mermaid 源码，并提供复制结果反馈。
- [x] 7.4 验证 Markdown 预览和 Mermaid 工具中的全屏切换、默认适配与复制行为。

## 8. 全屏工具栏布局稳定性

- [x] 8.1 将全屏缩放百分比调整为全屏容器右下角固定定位，关闭图标保持在右上角。
- [x] 8.2 验证图表 / 源码模式切换时缩放百分比不占用工具栏空间，也不推动工具栏操作按钮。

## 9. 全屏可见内容边界适配

- [x] 9.1 在全屏图表模式中基于 SVG 可见内容 bbox 归一化 viewBox，并据此计算默认 fit zoom。
- [x] 9.2 验证 SVG 声明 viewBox 含大量空白时，Markdown 预览和 Mermaid 工具的全屏图表可见内容仍能默认尽量占满容器。

## 10. 全屏右上角工具栏按钮完整命中

- [x] 10.1 修正全屏右上角工具栏中的图表、源码、复制、关闭四个控件，使每个控件整个可见按钮区域均可点击。
- [x] 10.2 验证 Markdown 预览和 Mermaid 工具中四个工具栏控件的边缘、角落和中心点击都能触发对应操作。
