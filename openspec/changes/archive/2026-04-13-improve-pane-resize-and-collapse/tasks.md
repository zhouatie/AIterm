## 1. SplitLayout 拖拽与持久化

- [x] 1.1 扩展 `src/components/SplitLayout.tsx`，支持从本地缓存或调用方参数初始化 `leftPercent`
- [x] 1.2 在 `SplitLayout` 拖拽状态下禁用左栏宽度、分隔条宽度和分隔条 margin 的 CSS transition
- [x] 1.3 在拖拽结束后保存合法的 `leftPercent`，并在读取缓存时按最小宽度约束夹取
- [x] 1.4 保持非拖拽的展开 / 收起布局变化仍可使用短过渡动画

## 2. 主左右分栏布局

- [x] 2.1 在 `src/App.tsx` 的主 `SplitLayout` 接入独立的主分栏比例缓存 key
- [x] 2.2 确认文件预览模块与终端模块之间的拖拽不再出现宽度动画滞后
- [x] 2.3 保持主分栏无缓存时使用当前默认比例，并保留最小宽度约束

## 3. 文件树内部收起与分栏缓存

- [x] 3.1 在 `src/components/FilePreviewPanel.tsx` 为文件树 / 预览内部分栏接入独立比例缓存 key
- [x] 3.2 为文件树内部面板新增展开 / 收起状态，并持久化到本地缓存
- [x] 3.3 在文件树面板展开和收起状态下显示对应 icon 控件与 tooltip
- [x] 3.4 保持 `FilePreviewPanel` 不注册 `toggle-file-tree` 或新增快捷键，内部文件树面板只通过 icon 切换
- [x] 3.5 保持 `App.tsx` 中 `toggle-file-tree` 继续用于切换整个文件系统 / 左侧模块
- [x] 3.6 文件树收起时保持文件预览内容可见，并暂停由 CWD 变化或 terminal tab 切换触发的目录读取
- [x] 3.7 文件树重新展开时读取当前活跃 terminal 的 CWD 并恢复文件树展示

## 4. 终端内容间距

- [x] 4.1 调整 `src/components/TerminalInstance.tsx` 或终端样式中的左侧内边距，避免 xterm 内容贴住左边界
- [x] 4.2 确认终端左侧间距不破坏 xterm 尺寸适配与 PTY resize 同步
- [x] 4.3 覆写 xterm viewport 滚动条样式，复用文件系统滚动条的宽度、轨道、滑块、hover 和主题变量

## 5. 快捷键行为确认

- [x] 5.1 确认 `toggle-file-tree` 默认绑定仍为 `Meta+S`，语义保持为整个文件系统 / 左侧模块展示与收起
- [x] 5.2 确认文件预览面板内部文件树收起不新增快捷键设置项
- [x] 5.3 确认终端获焦时 `Meta+S` 仍触发整个文件系统 / 左侧模块展示与收起，而不是内部文件树面板

## 6. 验证

- [ ] 6.1 手动验证主分栏拖拽时面板边界立即跟随鼠标，释放后布局稳定
- [ ] 6.2 手动验证文件树内部分栏拖拽时面板边界立即跟随鼠标，释放后布局稳定
- [ ] 6.3 手动验证重启应用后主分栏比例、文件树内部分栏比例和文件树收起状态恢复
- [ ] 6.4 手动验证文件树收起后左侧模块只保留文件预览区域，已选中文件内容不丢失
- [ ] 6.5 手动验证 terminal 左侧间距存在，且终端列宽与 TUI 显示没有回退
- [ ] 6.6 手动验证 xterm 终端滚动条与文件系统滚动条样式一致，并在明暗主题下都正确显示
