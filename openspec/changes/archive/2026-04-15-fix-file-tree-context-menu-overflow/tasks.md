## 1. 文件树菜单边界输入

- [x] 1.1 在 `FileTree.tsx` 为文件树可视容器增加稳定的 `ref`，右键触发时连同鼠标坐标一起采集宿主容器边界信息
- [x] 1.2 扩展文件树的 `contextMenu` 状态结构和 `ContextMenu` 调用参数，使文件树右键菜单能够收到可选的宿主边界数据

## 2. 右键菜单定位逻辑

- [x] 2.1 在 `ContextMenu.tsx` 增加可选边界参数，未传入时保持现有基于窗口视口的避让行为
- [x] 2.2 更新横向定位逻辑：文件树传入边界时优先依据宿主容器的 `left/right` 计算弹出方向，确保菜单不溢出到相邻面板
- [x] 2.3 保持菜单纵向避让、关闭行为、菜单项渲染和视觉样式不变，避免引入非本次修复范围的行为变化

## 3. 回归验证

- [x] 3.1 验证在普通宽度文件树中右键菜单仍在点击点附近弹出，复制路径和“在访达中显示”操作不受影响
- [x] 3.2 验证在窄文件树且靠近右侧边缘右键时，菜单会向左调整并完整显示在文件树面板内
- [x] 3.3 验证其他未传宿主边界的 `ContextMenu` 使用方继续按窗口视口规则工作，无新增定位回归
