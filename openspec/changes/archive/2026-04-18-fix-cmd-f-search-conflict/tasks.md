## 1. 快捷键焦点判定

- [x] 1.1 在现有快捷键处理路径中复用或新增终端键盘目标判定，明确识别 `.xterm` 内部事件来源
- [x] 1.2 确认真实输入框、设置面板快捷键录入框、文件预览查找框仍按现有规则跳过全局快捷键

## 2. 冲突修复

- [x] 2.1 更新 `TerminalPanel` 的 `Command + F` 监听，使其仅在终端（xterm.js）获得焦点时打开或切换终端搜索栏
- [x] 2.2 更新应用级 `find-in-file-preview` 触发逻辑，使其在事件来源位于终端（xterm.js）内部时不打开文件预览查找框
- [x] 2.3 保持 `Command + F` 默认绑定、用户自定义快捷键持久化结构和其他 terminal 管理快捷键行为不变

## 3. 验证

- [x] 3.1 手动验证终端聚焦时按 `Command + F` 只打开终端搜索栏，不打开文件预览查找框
- [x] 3.2 手动验证非终端区域按 `Command + F` 仍打开文件预览查找框，不打开终端搜索栏
- [x] 3.3 手动验证终端聚焦时 `Command + B`、`Command + T`、`Command + W` 等既有应用快捷键仍可触发
