## 1. 预览查找高亮重构

- [x] 1.1 移除 `src/utils/preview-find.ts` 中依赖 `TreeWalker` 和直接改写真实 DOM 的高亮实现，改为输出可供渲染层消费的纯数据匹配结果
- [x] 1.2 调整文件预览查找的公共接口，保留现有查询词、匹配计数和当前匹配项状态模型，但让滚动定位逻辑只读 DOM、不改 DOM

## 2. Markdown 与代码预览接入

- [x] 2.1 重构 `MarkdownPreview` 的查找高亮实现，使其在 React 渲染链路中生成匹配高亮，并保持 Markdown、GFM checkbox 和代码块行为不变
- [x] 2.2 重构 `CodePreview` 的查找高亮实现，使其在保留代码语法高亮的前提下渲染匹配结果，并支持当前项定位
- [x] 2.3 校准 `FilePreviewPanel` 与两类预览组件之间的匹配计数、当前项切换、关闭查找和切换文件重置行为，确保连续输入时状态一致

## 3. 稳定性验证

- [x] 3.1 手动验证 Markdown 预览中 `Command + F` 后快速连续输入、删除关键词不会触发未捕获渲染错误或内容区空白
- [x] 3.2 手动验证代码预览中快速连续输入和上一个、下一个导航不会导致预览区或主内容区消失
- [x] 3.3 回归验证现有查找行为仍成立：打开查找框、显示匹配计数、滚动到当前项、0 结果提示、关闭查找和切换文件重置
