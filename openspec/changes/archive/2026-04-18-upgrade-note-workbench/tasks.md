## 1. 工作台状态与数据模型重构

- [x] 1.1 将 NotePanel 从多 Tab 状态模型重构为单文档状态模型，明确 `activeView`、`selectedFolderPath`、`currentNotePath`、`searchQuery` 等核心状态
- [x] 1.2 新增笔记工作台本地持久化结构，保存收藏、最近访问记录与上次工作上下文
- [x] 1.3 调整当前笔记打开、切换、重命名、删除后的状态同步逻辑，去除旧的 Tab 相关依赖

## 2. Overlay 内工作台布局重做

- [x] 2.1 重构 NotePanel 布局为“视图栏 + 文件夹树 + 笔记列表 + 编辑区”的四段式结构，并保留 overlay 显隐与互斥规则
- [x] 2.2 重构文件夹树区域，保留强层级导航能力并支持当前文档路径高亮
- [x] 2.3 实现当前目录笔记列表与空状态视图，支持单文档打开流和相邻结果切换
- [x] 2.4 优化 overlay 内顶部/局部工具栏视觉与交互，提供新建笔记、新建文件夹、搜索与关闭等高频操作

## 3. 导航能力与高频交互完善

- [x] 3.1 实现 `最近`、`收藏`、`全部` 三个视图切换及其对应的数据源逻辑
- [x] 3.2 实现笔记收藏与取消收藏交互，并保证 `收藏` 视图结果稳定可恢复
- [x] 3.3 实现最近访问记录更新与工作台上下文恢复逻辑，处理失效路径的降级恢复
- [x] 3.4 调整新建、搜索、重命名、删除等交互在单文档工作台中的行为与反馈

## 4. 自动保存状态与收尾清理

- [x] 4.1 扩展 `note-autosave`，为当前文档提供 `编辑中 / 保存中 / 已保存` 状态输出
- [x] 4.2 在编辑区标题或状态区域接入自动保存状态展示，并确保文档切换、关闭 overlay、窗口失焦时正确 flush
- [x] 4.3 清理旧的多 Tab UI 与无效状态分支，补齐与新工作台模型对应的回归验证

## 5. UI 收敛修正

- [x] 5.1 收窄左侧视图栏、文件夹树和笔记列表的横向占比，确保编辑区保持主视觉优先级
- [x] 5.2 将笔记侧边栏的树交互调整为与文件系统模块文件树一致的展开、选中、悬停动作语法

## 6. 结构再收敛

- [x] 6.1 将最左侧视图栏继续收窄为仅保留 icon 的切换带，移除文字标签占位
- [x] 6.2 合并文件夹树与笔记列表为单一导航树，并让 `最近 / 收藏 / 全部` 共用这一棵树的展示结构
