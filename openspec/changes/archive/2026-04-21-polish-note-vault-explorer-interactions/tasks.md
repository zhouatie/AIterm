## 1. 右键菜单收敛

- [x] 1.1 重构笔记文件树的菜单构造逻辑，按空白区域、Vault 根节点、普通文件夹、文件四类上下文生成不同菜单项
- [x] 1.2 复用现有复制路径能力，为文件、文件夹和 Vault 根节点接入 `复制绝对路径` 与 `复制相对路径`
- [x] 1.3 为普通文件夹菜单加入 `收藏` 占位入口，并确保该入口不写入任何持久化或导航状态

## 2. 整行拖拽与高亮反馈

- [x] 2.1 移除文件树中的独立拖拽手柄，将文件和文件夹节点改为整行可拖
- [x] 2.2 为拖拽中的源节点增加持续高亮反馈，并为可接收的目标文件夹或 Vault 根节点增加目标高亮反馈
- [x] 2.3 保持现有 move/rename 路径同步逻辑可用，验证拖到文件夹、拖到 Vault 根节点和非法拖拽三类路径更新结果
- [x] 2.4 将文件夹整行点击语义调整为“更新当前选中文件夹并展开/收起”，并确保与整行拖拽不会互相误触
- [x] 2.5 将笔记树的字号、行密度和目录引导线对齐到文件系统树，并修正激活文件与目录定位态的双高亮问题

## 3. 交互回归验证

- [x] 3.1 验证 Vault 根节点右键菜单不再出现 `重命名`、`删除`、`收藏`
- [x] 3.2 验证文件右键菜单不再出现 `打开`，且文件夹菜单项顺序与 change spec 一致
- [x] 3.3 手动验证深色主题下拖拽源节点和目标节点的高亮可见性，必要时微调视觉样式
- [x] 3.4 验证文件夹整行点击和箭头点击都可以展开/收起，且不会破坏当前目录定位与拖拽交互
- [x] 3.5 验证笔记树字号、目录引导线和文件高亮优先级与文件系统树对齐，避免再次出现文件与文件夹同时高亮
