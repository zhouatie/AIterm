## 1. 统一 Tab 状态 schema 版本

- [x] 1.1 在 `tab-persistence` 模块中暴露唯一的当前 schema 版本定义，并让加载校验使用该定义
- [x] 1.2 提供由持久化模块注入当前版本的快照构建函数，避免调用方传入或硬编码版本号
- [x] 1.3 调整 `TerminalPanel` 的状态快照构建逻辑，使用统一入口生成当前版本数据且保持其余字段不变

## 2. 自动化回归覆盖

- [x] 2.1 新增 Tab 持久化工具测试，验证生成的快照携带当前 schema 版本
- [x] 2.2 验证字段完整的当前版本状态可以成功加载
- [x] 2.3 验证版本不匹配状态被拒绝且不执行兼容转换

## 3. 恢复链路验证

- [x] 3.1 运行 Tab 持久化相关 Node 测试，确认版本契约回归用例全部通过
- [x] 3.2 在应用中创建多个 workspace/terminal tab，退出并重新启动，确认树结构、活跃 Tab、重命名和侧边栏状态按当前版本快照恢复
