## 1. 快捷键与状态接入

- [x] 1.1 在 `ShortcutContext` 中新增文件预览内容查找 action、默认绑定与校验/持久化接入
- [x] 1.2 在 `FilePreviewPanel` 中注册文件预览查找动作，并新增统一的查找框显示、查询词、当前结果索引状态
- [x] 1.3 处理文件切换、查找框关闭与重复唤起时的状态重置和输入聚焦

## 2. 预览区查找交互

- [x] 2.1 在 `FilePreviewPanel` 中实现右侧预览区查找框 UI，以及结果计数、上一个/下一个、关闭交互
- [x] 2.2 将查找查询和导航状态下发给 `MarkdownPreview` 与 `CodePreview`，建立统一的预览查找接口
- [x] 2.3 确保查找框输入时不会重复触发全局快捷键，且不会误触左侧文件树搜索

## 3. Markdown 与代码预览查找渲染

- [x] 3.1 为 `MarkdownPreview` 实现基于当前关键词的匹配高亮和当前结果定位
- [x] 3.2 为 `CodePreview` 实现保留语法高亮前提下的匹配高亮和当前结果定位
- [x] 3.3 为两类预览实现结果导航滚动、0 结果反馈和高亮清理行为

## 4. 设置面板与验收

- [x] 4.1 在设置面板快捷键配置区展示文件预览内容查找动作
- [x] 4.2 检查新快捷键与现有快捷键列表、tooltip 文案和保存流程的一致性
- [x] 4.3 手动验证 `Command + F` 在 Markdown 与代码预览中的打开、输入、导航、关闭与文件切换重置行为
