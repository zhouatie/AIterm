## 1. 终端元信息与状态模型

- [x] 1.1 将 `TerminalPanel` 的扁平 tab 状态改为 workspace / session 双层树状状态，并引入递增的 `workspace_{index}` 计数器
- [x] 1.2 将现有仅返回 `cwd` 的终端接口升级为统一 session info 通道，提供 cwd 与 git 分支相关元信息，并同步改造 `FilePreviewPanel` 调用点
- [x] 1.3 实现二级终端标签名称的派生逻辑：git 仓库显示分支名，非 git 目录显示当前目录最后一级名称

## 2. 侧边导航与终端交互

- [x] 2.1 用左侧双层导航替换当前顶部 `TerminalTabBar`，支持 workspace 展开 / 收起与二级终端选中态
- [x] 2.2 实现顶部 `+` 仅新建 `workspace_{index}` + 首个二级终端的流程
- [x] 2.3 实现关闭二级终端、空 workspace 自动移除、关闭最后一个终端后自动补回默认 workspace 的行为
- [x] 2.4 为 terminal 侧边 tab 栏实现整体收起 / 展开能力，并在切换时为终端内容区重新分配宽度
- [x] 2.5 保持终端实例在切换过程中的挂载与状态保留，并在重新显示或侧边栏宽度变化后执行尺寸适配

## 3. 右键菜单与命名管理

- [x] 3.1 抽取文件树现有的右键菜单视图 / 通用路径动作，供 terminal tab 复用
- [x] 3.2 为一级 workspace 菜单增加 `New Tab` 和 `Rename`，并实现行内重命名流程
- [x] 3.3 为同 workspace 下的 `New Tab` 提供路径继承逻辑，默认复用该 workspace 的当前路径上下文
- [x] 3.4 为一级与二级节点接入单行省略号截断，避免长名称破坏侧边布局

## 4. 联调与验证

- [ ] 4.1 确认文件树仍能跟随当前活跃终端的 cwd 正常刷新
- [ ] 4.2 手工验证 git 仓库 / 非 git 目录下的二级标签命名是否符合规则
- [ ] 4.3 手工验证 workspace 重命名后不会被 cwd 变化覆盖
- [ ] 4.4 手工验证侧边栏收起 / 展开前后，workspace 展开态、活跃终端与终端会话都能被保留
- [ ] 4.5 手工验证 `+`、workspace 菜单 `New Tab`、关闭终端、自动滚动与右键菜单行为
