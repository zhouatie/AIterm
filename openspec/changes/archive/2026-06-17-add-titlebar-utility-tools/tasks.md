## 1. 依赖与结构

- [x] 1.1 在 `package.json` 和 lockfile 中新增 `mermaid` 前端依赖。
- [x] 1.2 新建统一开发者工具面板组件，定义 `json`、`qr`、`mermaid` 三种工具类型和内部状态结构。
- [x] 1.3 为工具面板建立大尺寸共享布局样式，包括遮罩、面板容器、工具标签、输入区、输出区、错误状态和操作按钮，确保 Mermaid 预览区足够清晰。

## 2. JSON 工具

- [x] 2.1 实现 JSON 输入区、输出区、格式化按钮、压缩按钮和复制输出按钮。
- [x] 2.2 使用原生 JSON API 完成格式化和压缩，格式化输出使用 2 空格缩进。
- [x] 2.3 在解析失败时展示错误信息，并保留用户输入和上一次成功输出。
- [x] 2.4 验证有效 JSON、无效 JSON、复制输出和关闭后重新打开状态保留行为。

## 3. 二维码工具

- [x] 3.1 复用 `qrcode` 在 renderer 中根据输入文本生成二维码 SVG。
- [x] 3.2 实现二维码空状态、加载/生成失败状态和浅色底面的二维码预览。
- [x] 3.3 实现复制二维码 SVG 操作，并在无二维码时禁用或隐藏复制入口。
- [ ] 3.4 验证输入生成、清空输入、生成失败提示和关闭后重新打开状态保留行为。

## 4. Mermaid 工具

- [x] 4.1 使用本地 `mermaid` 依赖初始化 renderer 端 Mermaid 渲染器，并启用严格安全配置。
- [x] 4.2 实现 Mermaid 源码输入、图表 SVG 预览、空状态和渲染错误展示。
- [x] 4.3 处理异步渲染竞态，只采纳当前输入对应的最新渲染结果。
- [x] 4.4 让 Mermaid 预览根据当前应用主题选择协调的 Mermaid 主题并在主题变化后重新渲染。
- [x] 4.5 验证有效图表、无效语法、空输入和主题切换后的预览行为。

## 5. 标题栏接入

- [x] 5.1 在 `App.tsx` 标题栏新增一个工具 icon-only 入口，并移除 JSON、二维码和 Mermaid 三个并列入口。
- [x] 5.2 复用现有标题栏按钮尺寸、hover 反馈、tooltip / `aria-label` 和 `WebkitAppRegion: 'no-drag'` 规则。
- [x] 5.3 点击工具入口时打开统一工具面板，首次默认显示 JSON，后续恢复同一会话内上次激活的工具视图。
- [x] 5.4 确认单一工具入口不会影响文件树开关、主题切换、Live View、更新检查和标题栏拖拽区域。

## 6. 验证

- [x] 6.1 运行项目 lint 或 TypeScript 检查，确保新增组件和依赖类型通过校验。
- [x] 6.2 启动本地 Electron 应用，手动验证单一顶部工具入口、面板切换、关闭和状态保留。
- [x] 6.3 在浅色、深色和跟随系统主题下验证大尺寸工具面板视觉、二维码可读性和 Mermaid 预览。
- [x] 6.4 在较窄窗口下验证标题栏单一工具入口、版本信息省略和既有标题栏按钮不重叠。
