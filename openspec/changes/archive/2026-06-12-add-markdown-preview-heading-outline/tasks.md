## 1. 标题收集

- [x] 1.1 在 `MarkdownPreview` 中定义标题树条目的内部数据结构，包含标题层级、展示文本、顺序索引和对应 DOM 元素标识。
- [x] 1.2 在 Markdown 内容渲染完成后，从当前预览根节点内查询 `h1` 到 `h6`，生成标题树条目列表。
- [x] 1.3 在 `content` 或 `filePath` 变化时重新收集标题，并在无标题时清空列表。
- [x] 1.4 确保标题收集基于渲染后的 DOM，不从源 Markdown 正则扫描标题。

## 2. 悬浮标题树 UI

- [x] 2.1 在 Markdown 预览滚动区域左侧渲染窄的悬浮标题树入口，仅当标题列表非空时显示。
- [x] 2.2 实现 hover 或 focus 后展开的标题树面板，并在鼠标离开或失焦后恢复收起状态。
- [x] 2.3 按标题层级渲染缩进、文本截断、hover 状态和当前主题变量样式，避免长标题溢出。
- [x] 2.4 让标题树浮层不参与正文布局，展开和收起时不改变 Markdown 正文宽度或换行。

## 3. 标题点击跳转

- [x] 3.1 为标题树每个标题项绑定点击处理，定位到对应的渲染标题 DOM。
- [x] 3.2 使用当前 Markdown 预览滚动容器执行滚动定位，使目标标题进入可视区域。
- [x] 3.3 确保点击标题不会改变选中文件、文件内容、文件树展开状态或 checkbox 写回状态。
- [x] 3.4 确保标题跳转与现有预览查找滚动逻辑互不共享状态、不产生渲染崩溃。

## 4. 验证

- [x] 4.1 验证包含 `h1` 到 `h6` 的 Markdown 文档能显示层级标题树，并能点击跳转到对应标题。
- [x] 4.2 验证代码块中的 `#` 文本不会出现在标题树中。
- [x] 4.3 验证无标题 Markdown 文件不显示标题树入口。
- [x] 4.4 验证非 Markdown 文件仍使用代码预览模式且不显示标题树。
- [x] 4.5 验证现有 Markdown checkbox 写回、预览区查找和普通滚动浏览行为保持可用。
- [x] 4.6 运行项目现有静态检查或构建命令，确认没有新增 TypeScript 或 lint 问题。
